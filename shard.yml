name: mailcatcher
version: 0.10.0

authors:
  - Crystal port of MailCatcher by Samuel Cochran

description: |
  MailCatcher runs a super simple SMTP server which catches any
  message sent to it to display in a web interface.

targets:
  mailcatcher:
    main: src/mailcatcher.cr

dependencies:
  kemal:
    github: kemalcr/kemal
    version: ~> 1.8.0

crystal: ">= 1.18.0"

license: MIT
